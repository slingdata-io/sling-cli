name: {{NAME}}
# project_id: set by `sling project deploy` when this folder is linked

# jobs:
#   daily:
#     file: pipelines/daily.yaml
#     schedules: ["0 6 * * *"]
#     # run locally on demand: sling run -j daily
#     # schedules fire on the platform after 'sling project deploy'
