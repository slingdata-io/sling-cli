source: GOOGLE_STORAGE
target: BIGQUERY

defaults:
  object: 'public.{stream_file_name}_slack_data'
  source_options:
    flatten: true
  target_options:
    adjust_column_type: true
  mode: full-refresh

streams:
  gs://flarco_us_bucket/MLOps.community.test/mlops-questions-answered/:
  gs://flarco_us_bucket/MLOps.community.test/mlops-special-project-berlin/:
  gs://flarco_us_bucket/MLOps.community.test/channels.json:
  gs://flarco_us_bucket/MLOps.community.test/integration_logs.json: