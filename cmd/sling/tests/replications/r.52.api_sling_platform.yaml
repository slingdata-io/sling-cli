source: sling_platform_api_staging
target: postgres

defaults:
  object: '{source_name}.{stream_name}'
  mode: full-refresh

streams:
  '*':
  executions_many:
    mode: incremental

env:
  SLING_STATE: postgres/sling_state.sling_platform_api_staging