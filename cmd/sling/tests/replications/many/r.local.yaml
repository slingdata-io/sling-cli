source: local
target: postgres

hooks:
  start:
    - command: |
        echo 'a,b,c
        1,2,3
        4,5,6' > /tmp/out.csv

  end:
    - connection: postgres
      query: drop table if exists public.out

streams:
  file:///tmp/out.csv:
    object: public.out