connections:
  MY_PG:
    type: postgres
    host: localhost
    user: sling
    database: sling
    password: LiteralPass123ABC
    secret_access_key: ${MY_VALIDATE_SECRET}
  MY_API:
    type: api
    secrets:
      token: LiteralTokenXYZ
env:
  FOO: bar
