name: MOCK_API
authentication:
  type: static
  headers:
    Authorization: "Bearer nope"
endpoints:
  users:
    request:
      url: "{secrets.base_url}/users"
      method: GET
    response:
      records:
        jmespath: "data"
