steps:
  - type: list
    location: LOCAL/fixtures/data/ecom/*.csv
    only: files
  - type: group
    loop: state.list.result
    steps:
      - type: query
        connection: DUCKDB
        query: select 1
